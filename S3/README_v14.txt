myHome v14 - 주식 로더 실행 버그 수정

원인:
v13에는 loadStocksDirect() 함수가 정의되어 있었지만
페이지가 열릴 때 실제로 호출하는 코드가 없었습니다.

증상:
- '시세 상태: 데이터 대기'
- '시세 출처: 네이버 금융 직접조회 준비중'
- 계속 그대로 유지

v14 수정:
- DOM 로딩 완료 시 loadStocksDirect() 즉시 실행
- 이후 60초마다 자동 재조회
- 화면 상단에 v14 표시
- PWA/Service Worker 캐시 버전 myhome-v14로 변경

중요:
ESP32 펌웨어는 다시 올릴 필요 없습니다.
GitHub Pages 파일만 전부 덮어쓰기 하세요.

정상 동작 시 시세 출처가 순서대로:
네이버 금융 초기화 중
→ 네이버 금융 조회 중...
→ 네이버 금융 realtime 또는 네이버 금융 JSONP
로 바뀝니다.

만약 '네이버 금융 직접조회 실패'로 바뀌면,
그때는 조회는 실제 실행됐고 네트워크/API 단계에서 실패한 것입니다.
